[package]
name = "mads-conditional-route-consumer"
version = "0.0.0"
edition = "2024"
rust-version = "1.94"

[features]
conditional-route = []

[dependencies]
mads = { path = "../../.." }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tower = { version = "0.5", features = ["util"] }

[workspace]
