[package]
name = "mads-macro-facade-consumer"
version = "0.0.0"
edition = "2024"
rust-version = "1.94"

[dependencies]
mads = { path = "../../..", default-features = false, features = ["common"] }

[workspace]
