[package]
name = "inspection-database-fixture"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"

[dependencies]
mads = { path = "../../../../../mads", default-features = false, features = ["database", "http", "runtime-tokio"] }

[workspace]
