[package]
name = "matrix-single"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"

[dependencies]
mads = { path = "../../../../../mads", default-features = false, features = ["http", "runtime-tokio"] }

[workspace]
